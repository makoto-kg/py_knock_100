import numpy as np


def exec():
    color = np.dtype([("r", np.ubyte),
                      ("g", np.ubyte),
                      ("b", np.ubyte),
                      ("a", np.ubyte)])
    print(color)
